// 변수 value 선언
const value = 0;

// 연산 결과를 변수 result의 초기값으로 대입
let result = value + 10;

// 변수 result 값을 읽고 콘솔에 출력
console.log(result);

let hour = 3;
let minute = 5;
console.log(hour + "시간 " + minute + "분 ");

let totalMinute = (hour*60) + minute;
console.log("총 " + totalMinute + "분 ");